# on-auction

Firebase Auth + Firestore 기반 경매 웹앱입니다.

## 실행 방법

```bash
npm install
cp .env.example .env.local
npm run dev
```

브라우저에서 `http://localhost:3000` 접속.

`.env.local`에 Firebase 웹앱 키를 실제 값으로 넣어야 실행됩니다.

## 주요 기능

- 구글 로그인/로그아웃
- 상품 등록/수정/삭제
- 실시간 입찰 및 입찰 로그 저장
- 즉시구매가 연동 로직
- 마감 카운트다운/마감 후 입찰 차단
- 경매 종료 후 7일 보관, 이후 자동 삭제(Cloud Functions Scheduler)

## 경매 종료 후 7일 자동삭제

`products` 문서에 `expireAt`(밀리초 타임스탬프)을 저장하고, 만료된 상품은 화면에서 숨깁니다.
서버 스케줄러가 매일 실행되어 만료 상품과 하위 `bids` 로그를 삭제합니다.

### 1) Functions 의존성 설치

```bash
cd functions
npm install
```

### 2) Firebase 로그인

```bash
npx firebase login
```

### 3) Functions 배포

```bash
npx firebase deploy --only functions
```

### 3-1) Firestore Rules/Indexes 배포

```bash
npx firebase deploy --only firestore:rules,firestore:indexes
```

### 4) 동작 확인

- 스케줄 함수: `cleanupExpiredProducts`
- 주기: 매일 03:00 (`Asia/Seoul`)
- 대상: `expireAt <= now` 인 상품

## 참고

- Next.js 앱 코드: `app/page.tsx`
- Firebase 클라이언트 설정: `lib/firebase.ts`
- 자동삭제 함수: `functions/index.js`
- Firestore 보안 규칙: `firestore.rules`
- Firestore 인덱스 파일: `firestore.indexes.json`
